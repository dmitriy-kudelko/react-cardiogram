import { CardiogramProps } from './types'

const defaults: CardiogramProps = {
  width: 500,
  height: 100,
  color: '#22ff22',
  thickness: 2,
  scale: 35,
  cursorSize: 3,
  density: 2,
  paintInterval: 30
}

export default defaults
